import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
   signupContainer: {
        flexDirection: 'row',
        justifyContent: 'center',
        alignItems: 'center',
    },
    signupText: {
        color: '#666',
        fontSize: 14,
    },
    signupLink: {
        color: '#00CCA7',
        fontSize: 14,
        fontWeight: '500',
    }
})