import { StyleSheet } from 'react-native';

export const styles =  StyleSheet.create({
    appleButton: {
        backgroundColor: '#000',
        borderRadius: 8,
        padding: 15,
        alignItems: 'center',
    },
    appleButtonText: {
        color: 'white',
        fontSize: 16,
        fontWeight: '500',
    },
})